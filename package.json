{
  "name": "sudoblog",
  "private": true,
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "docker:build": "docker build -t sudoblog .",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  }
}
